import Vue from 'vue';
import Vuetify from 'vuetify';
import SkeletonName from '../skeleton-name';
import { mount } from '@vue/test-utils';

describe('SkeletonName', () => {
  Vue.use(Vuetify);

  describe('props', () => {});

  describe('events', () => {});
});
